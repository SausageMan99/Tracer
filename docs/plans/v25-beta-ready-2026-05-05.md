# TrailForge V2.5 Beta Ready — Plan d'action jusqu'au 2026-05-05 soir

> **Pour Hermes:** exécuter avec une seule lane code sur `/root/work/Tracer`; diagnostics/review en parallèle uniquement en lecture. Ne pas créer de crons récursifs. Ne pas baisser les seuils. Ne pas reclassifier asphalt/concrete/paved scenic en trail.

**Goal:** rendre TrailForge V2.5 déployable en bêta Vercel demain soir, avec moteur honnête, routes invalides refusées proprement, smoke stable, et collecte de feedback testeurs.

**Architecture:** court terme V2.5 = durcir la sélection finale et l'observabilité sans refonte V3. Tourville est traité comme benchmark qualité avancé: stabilisation prioritaire, mais si non résolu à 100%, il doit être isolé par fallback/flag/refus propre pour ne pas bloquer la bêta globale.

**Tech Stack:** Next.js/Vercel, TypeScript, route engine TrailForge, benchmarks live `npm run benchmark:routes:*`, Hermes Kanban profil `trailforge`.

---

## Verdict cible

Demain soir, viser **bêta contrôlée**, pas GA stable.

Go bêta si:
- `git diff --check`, `npx tsc --noEmit`, `npm run lint`, `npm run test:run`, `npm run build` passent.
- Sprint 4 smoke passe 5/5 au moins 3 runs propres.
- L'API ne sert jamais en `success` une route qui viole les hard gates qualité.
- Tourville est soit stable sur 8k/12k, soit protégé par refus/fallback clair.
- Le déploiement Vercel est vérifié par un test réel POST `/api/generate-route`.
- Les testeurs reçoivent une consigne simple et peuvent remonter captures + feedback.

No-go si:
- build ou tests rouges;
- 500 API;
- hard-gate violation servie comme route valide;
- erreurs `NO_ROAD_NETWORK` mensongères alors que des candidats ont été rejetés;
- diff impossible à comprendre / commit non isolable.

---

## État initial au 2026-05-04 16:49 Paris

Branche: `feat/p1-2-quality-ratio-interpretation`, ahead 2.

Repo très dirty: engine, API, scripts benchmarks, tests, docs. Donc:
- pas de multiple code writers;
- un seul agent/lane modifie;
- review/QA uniquement après stabilisation;
- commit uniquement fichiers intentionnels.

Un dispatcher Kanban profil `trailforge` tourne déjà:
`while true; do hermes --profile trailforge kanban dispatch --max 1 ...; done`

---

## Brainstorm synthèse

### Option A — Bloquer toute V2.5 sur Tourville
Qualité stricte, mais mauvais choix business si Tourville est isolable. Risque: encore 48h à tuner un cas pendant que zéro utilisateur teste.

### Option B — Déployer V2.5 bêta hors Tourville
Bon choix si garde-fous prêts. Tourville reste benchmark interne, pas promesse externe.

### Option C — Déployer V2.5 avec Tourville sous fallback/refus propre
Meilleur compromis: si Tourville échoue hard gates, l'utilisateur voit un refus propre ou une limitation bêta, jamais une route mauvaise.

### Option D — Corriger Tourville structurellement avant deploy
À tenter ce soir/demain matin, mais borné. Si ça dépasse, ne pas sacrifier la bêta globale.

Décision retenue: **Option C**, avec tentative de stabilisation Tourville en amont.

---

## Workstream 1 — Stabilisation moteur Tourville / hard gates

### Task 1.1 — Inspecter les derniers rouges Tourville

**Objective:** confirmer la cause exacte avant tout patch.

**Files:**
- Read: `artifacts/route-benchmark-results/warroom-tourville-20260504e-run-3.json`
- Read: `artifacts/route-benchmark-results/warroom-tourville-20260504f-run-1.json`
- Read route/failure artifacts associés.

**Commands:**
```bash
python - <<'PY'
import json
from pathlib import Path
for name in ['warroom-tourville-20260504e-run-3.json','warroom-tourville-20260504f-run-1.json']:
    p=Path('artifacts/route-benchmark-results')/name
    d=json.loads(p.read_text())
    print('\n==', name, '==')
    print(d.get('summary'))
    for r in d.get('results',[]):
        print(r.get('caseId'), r.get('passed'), r.get('failures') or r.get('failedCriteria'), r.get('error'))
PY
```

**Expected:** causes rouges exactes: paved_ratio / u_turn_ratio / quality_gate_failed.

---

### Task 1.2 — RED test hard-gate-first ranking

**Objective:** empêcher un candidat haut score mais rouge `u_turn_ratio` de battre un candidat valide.

**Files:**
- Modify: `tests/route-gate-selector.test.ts`
- Modify: `tests/post-process-ranking.test.ts`
- Target code: `lib/engine/route-gate-selector.ts`, `lib/engine/route-post-processor.ts`

**Test intent:**
- candidat A: meilleur score brut, `uTurnRatio > max`, doit être rejeté/dépriorisé.
- candidat B: score brut inférieur, tous hard gates OK, doit être sélectionné.

**Run:**
```bash
npm run test:run -- tests/route-gate-selector.test.ts tests/post-process-ranking.test.ts
```

**Expected before fix:** FAIL.

---

### Task 1.3 — Implémenter ranking hard-gates avant score composite

**Objective:** sélectionner d'abord par validité produit, ensuite par plaisir/scoring.

**Files:**
- Modify: `lib/engine/route-gate-selector.ts`
- Modify: `lib/engine/route-post-processor.ts`

**Rules:**
- bucket 0: tous hard gates OK;
- bucket 1: violations mineures relaxables explicitement autorisées;
- bucket 2: hard-gate failure, jamais route `success`;
- dans chaque bucket seulement: trier par productionScore/trailBeauty/distance/D+.

**Verification:**
```bash
npm run test:run -- tests/route-gate-selector.test.ts tests/post-process-ranking.test.ts
```

---

### Task 1.4 — RED test micro-spur / A-B-A repair

**Objective:** réduire les faux rouges `u_turn_ratio` quand le problème est local et réparable.

**Files:**
- Modify: `tests/orienteering-solver.test.ts`
- Modify: `tests/route-quality.test.ts`
- Target code: `lib/engine/orienteering-solver.ts`, `lib/engine/route-post-processor.ts`, `lib/engine/route-quality.ts`

**Test intent:** une route avec motif `A-B-A` court doit être détectée comme spur/demi-tour parasite; si suppression conserve distance/géométrie dans tolérance, la route réparée doit passer.

**Verification:**
```bash
npm run test:run -- tests/orienteering-solver.test.ts tests/route-quality.test.ts
```

---

### Task 1.5 — Implémenter réparation locale bornée

**Objective:** réparer uniquement les motifs simples sans masquer les métriques.

**Rules:**
- détecter `A-B-A`, aller-retour court, spur court;
- tenter suppression/remplacement local;
- recalculer distance, loop closure, repeat, uTurn, paved;
- si distance/D+/loop sortent tolérance: refuser;
- logguer `repairApplied: true`, `repairType`, delta metrics dans diagnostics benchmark seulement.

**Verification:**
```bash
npm run test:run -- tests/orienteering-solver.test.ts tests/route-quality.test.ts tests/post-process-ranking.test.ts
```

---

### Task 1.6 — RED test Tourville-like soft component targeting

**Objective:** empêcher le planner de cibler un parent forest/scenic pollué quand un sous-corridor soft existe.

**Files:**
- Modify: `tests/terrain-planner.test.ts`
- Target code: `lib/engine/terrain-planner.ts`

**Test intent:** composant parent avec asphalt/concrete connectors + child soft path/track/ground. Pour `transition_to_woods`, `targetComponents` doit prioriser le soft child avec `pavedKm=0`, pas le parent pollué.

**Verification:**
```bash
npm run test:run -- tests/terrain-planner.test.ts
```

---

### Task 1.7 — Implémenter ciblage soft prioritaire pour `transition_to_woods`

**Objective:** rendre Tourville plus stable en forçant l'intention vers les vrais corridors boisés.

**Files:**
- Modify: `lib/engine/terrain-planner.ts`

**Rules:**
- si parent naturel/scenic contient soft subcomponents exploitables, les soft children deviennent cibles primaires;
- parent reste contexte/connecteur, pas objectif principal;
- score cible = nonPavedKm + unknownNaturalKm + distance start raisonnable + entry/exit connectivity - pavedKm;
- pas de reclassement surfaces.

**Verification:**
```bash
npm run test:run -- tests/terrain-planner.test.ts tests/route-benchmarks.test.ts
```

---

## Workstream 2 — API/fallback bêta honnête

### Task 2.1 — Vérifier les erreurs actuelles hard gate

**Objective:** éviter les faux `NO_ROAD_NETWORK` quand le graphe existe mais les candidats sont rejetés.

**Files:**
- Read/Modify: `app/api/generate-route/route.ts`
- Read/Modify: `lib/errors.ts`
- Read/Modify: `lib/engine/route-gate-selector.ts`
- Test: `tests/generate-route-api.test.ts`
- Test: `tests/engine-failure-classification.test.ts`

**Expected API semantics:**
- `ROUTE_CANDIDATES_REJECTED` ou équivalent si candidats rejetés par hard gates;
- diagnostic derrière flag interne/benchmark;
- message utilisateur clair: “Aucun parcours ne respecte les critères qualité pour cette demande.”

---

### Task 2.2 — RED tests erreurs typées

**Objective:** garantir qu'une route invalide ne sort jamais en `success`.

**Test cases:**
- candidats existent mais tous échouent `paved_ratio` → error typée quality rejection;
- candidats existent mais tous échouent `u_turn_ratio` → error typée quality rejection;
- absence graphe réelle → `NO_ROAD_NETWORK` conservé.

**Run:**
```bash
npm run test:run -- tests/generate-route-api.test.ts tests/engine-failure-classification.test.ts
```

---

### Task 2.3 — Implémenter garde-fou bêta

**Objective:** en bêta, refuser proprement plutôt que servir mauvais.

**Rules:**
- hard gates non relaxables: paved, repeat, uTurn, distance;
- pas de HTTP 200 success si hard gate red;
- diagnostics légers disponibles pour debug;
- pas de fuite massive edgeDiagnostics dans réponse publique par défaut.

**Verification:**
```bash
npm run test:run -- tests/generate-route-api.test.ts tests/engine-failure-classification.test.ts
```

---

## Workstream 3 — Validation locale release candidate

### Task 3.1 — Gates techniques

**Commands:**
```bash
git diff --check
npx tsc --noEmit
npm run lint
npm run test:run
npm run build
```

**Expected:** all green. Sinon stop.

---

### Task 3.2 — Serveur frais local

**Commands:**
```bash
HOSTNAME=127.0.0.1 PORT=3015 npm run start
curl -sS -o /tmp/tf_health.txt -w '%{http_code}\n' http://127.0.0.1:3015/api/generate-route
```

**Expected:** process alive; GET endpoint peut être 405; pas 500.

**Process hygiene:** tuer le serveur après benchmarks.

---

### Task 3.3 — Sprint 4 smoke x3

**Commands:**
```bash
for i in 1 2 3; do
  ROUTE_BENCHMARK_BASE_URL=http://127.0.0.1:3015 \
  npm run benchmark:routes:sprint4-smoke -- \
    --save-artifacts \
    --output artifacts/route-benchmark-results/v25-beta-smoke-20260505-run-$i.json \
    --artifact-dir artifacts/route-benchmark-results/v25-beta-smoke-20260505-run-$i-routes \
  || exit 1
done
```

**Expected:** 5/5 x3.

---

### Task 3.4 — Tourville standalone stability

**Commands:**
```bash
for i in 1 2 3 4 5; do
  ROUTE_BENCHMARK_BASE_URL=http://127.0.0.1:3015 \
  npm run benchmark:routes -- \
    --case tourville-pommiers-trail-8k \
    --case tourville-pommiers-trail-12k \
    --save-artifacts \
    --output artifacts/route-benchmark-results/v25-beta-tourville-20260505-run-$i.json \
    --artifact-dir artifacts/route-benchmark-results/v25-beta-tourville-20260505-run-$i-routes \
  || break
done
```

**Interpretation:**
- 5/5 runs 2/2 = Tourville peut être considéré beta-safe.
- Échec qualité propre = bêta possible avec Tourville limitation/fallback.
- Route invalide servie success = no-go.

---

## Workstream 4 — Commit/release hygiene

### Task 4.1 — Isoler le diff

**Commands:**
```bash
git status --short --branch
git diff --stat
git diff --check
```

**Rules:**
- Ne commit que fichiers intentionnels.
- Docs historiques non nécessaires à deploy peuvent rester hors commit si elles polluent.
- Aucun artifact benchmark lourd dans commit sauf si projet les versionne explicitement.

---

### Task 4.2 — Commit release candidate

**Commands:**
```bash
git config user.name "SausageMan99"
git config user.email "clement.dubosq@wanadoo.fr"
git add app/api/generate-route/route.ts lib/engine lib/errors.ts lib/types.ts lib/route-benchmarks* scripts tests package.json

git commit -m "fix: harden v25 beta route quality gates"
```

**Only if:** gates + smoke pass and beta guardrails valid.

---

### Task 4.3 — Push branch / Vercel deploy

Preferred for beta: push branch and deploy Preview or production beta domain.

**Commands:**
```bash
git push origin feat/p1-2-quality-ratio-interpretation
vercel --prod
```

**Only if:** Clément accepte production beta. Sinon `vercel` preview.

---

## Workstream 5 — Post-deploy verification

### Task 5.1 — Smoke API Vercel

**Objective:** vérifier que Vercel n'a pas un comportement différent du local.

**Manual/API tests:**
- 1 route facile autour Caen/Meudon;
- 1 route trail 8-10k;
- 1 cas Tourville si flag/fallback voulu;
- vérifier que les refus sont propres.

**Expected:** pas de 500, pas de route hard-gate red en success.

---

### Task 5.2 — Feedback loop testeurs

**Message testeurs:**

“Je teste une nouvelle version bêta du moteur TrailForge. Le moteur peut refuser une génération s’il estime que la route serait trop bitumée, trop répétitive ou pas assez qualitative. Teste 1 à 3 parcours autour de chez toi entre 5 et 15 km. Envoie-moi: capture, ville/distance demandée, ce qui est bien, ce qui est nul, trop de bitume ou non, demi-tours/boucles bizarres, et si tu courrais vraiment cette route.”

**Feedback format:**
- Ville / départ approximatif;
- Distance demandée;
- Screenshot route;
- Note envie de courir /10;
- Trop de bitume ? oui/non;
- Boucles ou demi-tours visibles ? oui/non;
- Commentaire libre.

---

## Planning horaire recommandé

### Lundi soir 2026-05-04
- 17:00–18:00: finaliser diagnostic diff + rouges Tourville.
- 18:00–21:00: implémentation hard-gate-first + anti-U-turn repair.
- 21:00–23:00: soft component targeting + tests unitaires.
- 23:00–00:00: API errors/fallback beta.

### Nuit
- Gates techniques.
- Smoke local.
- Premier Tourville standalone.
- Rapport no-go/go intermédiaire.

### Mardi matin 2026-05-05
- Corriger uniquement blockers simples et prouvés.
- Relancer gates.
- Relancer smoke x3.

### Mardi après-midi
- Tourville standalone 5 runs.
- Si Tourville échoue proprement: activer limitation/fallback beta.
- Commit propre.
- Push branch.

### Mardi soir
- Déploiement Vercel.
- Vérification API prod.
- Envoi aux testeurs.
- Brief Clément: liens, état des gates, limitations connues.

---

## Décision finale attendue demain soir

### Best case
Tourville 8k/12k stable 5 runs, smoke 5/5 x3, deploy Vercel, bêta ouverte.

### Likely case
Smoke stable, Tourville encore borderline mais refus propre. Deploy bêta avec Tourville sous limitation/fallback. C'est acceptable pour apprendre.

### Bad case
Hard gates rouges servis en success, ou build/API instable. No-go deploy, mais rapport clair et prochaine correction ciblée.

---

## Principe directeur

Demain soir, TrailForge doit être **testable honnêtement**, pas parfait. Le moteur doit préférer refuser plutôt que mentir. La bêta sert à apprendre des utilisateurs; Tourville sert à durcir le moteur.